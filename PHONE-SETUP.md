# 핸드폰에서 쓰는 방법

이 앱은 정적 웹앱이라 GitHub Pages, Netlify, Vercel 같은 무료 호스팅에 올리면 핸드폰에서 바로 열 수 있습니다.

## 추천 방식

1. `cook` 폴더를 GitHub 저장소에 올립니다.
2. GitHub Pages를 켜서 `index.html`이 열리는 URL을 만듭니다.
3. 핸드폰 Safari 또는 Chrome에서 그 URL을 엽니다.
4. 공유 버튼 또는 브라우저 메뉴에서 `홈 화면에 추가`를 선택합니다.

이후에는 홈 화면 아이콘을 눌러 앱처럼 사용할 수 있습니다.

## 참고

- `file://`로 연 페이지는 핸드폰 홈 화면 설치와 오프라인 캐시가 제대로 동작하지 않습니다.
- PWA 설치는 보통 HTTPS 주소에서 동작합니다.
- 사진 OCR은 Tesseract.js 모델을 처음 불러올 때 인터넷 연결이 필요할 수 있습니다.

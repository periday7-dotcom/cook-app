# 냉장고 식단 플래너 작업 내역

이 문서는 지금까지 진행한 작업을 날짜별로 정리한 기록입니다. 기준 날짜는 파일 수정일과 대화에서 진행한 작업 흐름을 함께 반영했습니다.

앞으로 이 프로젝트에서 작업을 마무리할 때마다 이 문서를 함께 업데이트합니다.

## 2026-06-11

### 모바일 사용 환경 준비

- 기존 웹앱을 휴대폰에서 접근하기 쉽도록 PWA 형태로 정리했습니다.
- `manifest.json`을 추가해 홈화면 추가가 가능한 앱 형태를 구성했습니다.
- `sw.js` 서비스워커를 추가해 앱 셸 캐싱을 적용했습니다.
- `app-icon.svg` 앱 아이콘을 추가했습니다.
- GitHub Pages 또는 정적 배포 환경에서 동작할 수 있도록 `.nojekyll`을 추가했습니다.

### 사용 안내 문서 작성

- `PHONE-SETUP.md`를 작성해 휴대폰 홈화면 추가 방법을 정리했습니다.
- `DEPLOY.md`를 작성해 GitHub 업로드와 배포 흐름을 안내했습니다.
- Netlify Drop 방식의 1시간 제한 문제를 확인하고, GitHub Pages 방식으로 전환하는 방향을 안내했습니다.

## 2026-06-14

### 레시피 자료 조사

- 한식 레시피 자료 조사를 진행했습니다.
- 인터넷과 유튜브 등 다양한 출처 기반으로 레시피 후보를 정리했습니다.
- 조사 결과를 마크다운 표 형식으로 저장했습니다.

### 생성된 자료 파일

- `korean-recipes-200.md`
- `korean-recipes-diverse-sources.md`

## 2026-06-18

### 식단 생성 기능 개선

- 하루 세 끼가 아니라 점심/저녁 두 끼 기준으로 식단을 생성하도록 변경했습니다.
- 일주일 기준 총 14끼 식단을 생성하도록 조정했습니다.
- 식사별 조리시간을 추가했습니다.
- 요리 난이도 비율을 조절할 수 있도록 점심/저녁 간편식 비율 슬라이더를 추가했습니다.
- “또 다른 버전의 일주일 식단 짜기” 버튼을 추가했습니다.
- 카테고리 비중 조절 기능을 추가했습니다.
  - 한식
  - 양식
  - 일식
  - 중식
  - 기타
- “완전 랜덤으로 짜기” 옵션을 추가했습니다.

### 재료 입력 UI 개선

- 보유 재료 입력칸을 보관 방식별로 나눴습니다.
  - 냉장
  - 냉동
  - 실온보관
  - 양념류
- 재료를 긴 텍스트로 나열하는 방식 대신 칩 형태로 추가/삭제할 수 있게 변경했습니다.
- 입력한 재료가 브라우저 저장소에 유지되도록 했습니다.
- 밀키트/완제품 입력칸도 재료 입력과 같은 칩 UI로 변경했습니다.
- 밀키트/완제품도 식단 후보에 포함되도록 연결했습니다.

### 사진/OCR 입력 기능

- 재료 사진 업로드 UI를 추가했습니다.
- Tesseract 기반 OCR을 연결해 사진에서 한글/영문 재료명을 읽어올 수 있게 했습니다.
- OCR 결과를 직접 수정한 뒤 재료 칩에 반영할 수 있도록 했습니다.
- 한글 인식률이 완벽하지 않은 점을 고려해 수동 보정 흐름을 함께 유지했습니다.

### 레시피 데이터 확장

- 대량 레시피 데이터 파일을 구성했습니다.
- `recipes-data.json`에 총 1,420개 레시피를 저장했습니다.
- 카테고리별 레시피 수는 다음과 같습니다.

| 카테고리 | 개수 |
|---|---:|
| 한식 | 740 |
| 양식 | 320 |
| 일식 | 120 |
| 중식 | 120 |
| 기타 | 120 |

- `recipes-data-summary.json`을 추가했습니다.
- `recipes-data.js`를 추가해 JSON fetch가 실패하는 환경에서도 대량 레시피를 불러올 수 있도록 했습니다.

### 전체 레시피 관리 기능

- 전체 레시피 관리 탭을 추가했습니다.
- 레시피를 카테고리별로 필터링할 수 있게 했습니다.
- 레시피명, 카테고리, 재료명으로 검색할 수 있게 했습니다.
- 기본 레시피를 수정/삭제/복구할 수 있게 했습니다.
- 직접 만든 레시피를 추가/수정/삭제할 수 있게 했습니다.
- 대량 레시피 파일이 로드되지 않을 때 기본 22개 레시피만 표시되는 문제를 확인했습니다.
- `recipes-data.js` 우회 로딩을 추가해 홈화면 앱/로컬 환경에서도 1,420개 레시피가 로드되도록 개선했습니다.
- 관리 화면에서 처음 120개만 보여주고, “레시피 더 보기” 버튼으로 120개씩 추가 표시되도록 변경했습니다.

### 출처와 링크

- 식단에 레시피 출처 영역을 추가했습니다.
- 각 식단에 네이버 레시피 검색 링크와 유튜브 영상 검색 링크를 표시했습니다.
- 캘린더에 들어갈 내용에도 레시피 이름, 링크, 추가 구매 재료가 들어가도록 구성했습니다.

### 캘린더 연동

- 생성된 일주일 식단을 기본 캘린더에 넣을 수 있도록 `.ics` 파일 다운로드 기능을 추가했습니다.
- 캘린더 이벤트에는 다음 내용을 포함하도록 했습니다.
  - 레시피 이름
  - 레시피 검색 링크
  - 추가 구매가 필요한 재료
  - 조리시간
  - 카테고리

### 예산 계산 개선

- 추가 재료 예산 계산 기능을 추가했습니다.
- 기존의 임의 가격표가 실제 장보기 가격대와 많이 달라서, 대형마트/온라인몰 소포장 기준에 가까운 가격표로 확장했습니다.
- 같은 재료가 여러 끼에 반복되어도 일주일 예산에서는 중복 계산하지 않도록 수정했습니다.
- `간편`, `매운`, `집밥`, `원팬`, `Soup`처럼 구매 재료가 아닌 태그는 예산 계산에서 제외했습니다.
- 각 추가 재료마다 가격 확인 링크를 추가했습니다.
  - 네이버
  - 쿠팡
  - 이마트
  - 롯데마트

### 모바일 UI 개선

- 휴대폰 사용을 우선한 모바일 레이아웃으로 정리했습니다.
- 홈화면 앱으로 사용할 때 주요 버튼이 접근하기 쉽게 하단 영역을 조정했습니다.
- 입력 영역, 레시피 관리 영역, 식단 결과 영역이 모바일에서 세로로 자연스럽게 이어지도록 조정했습니다.

### 디자인 개선

- 기존의 탁한 초록/베이지 중심 색상을 파스텔톤으로 변경했습니다.
- 전체 색상 팔레트를 민트, 살구, 라벤더, 크림 계열로 조정했습니다.
- 버튼, 칩, 카드, 가격 링크, 모바일 상단바 색상을 통일했습니다.
- 홈화면 앱의 `theme-color`, `manifest.json`, `app-icon.svg`도 새 파스텔 톤에 맞게 수정했습니다.

### 캐시와 배포 파일

- 서비스워커 캐시 버전을 여러 차례 갱신했습니다.
- 최종 캐시 버전은 `fridge-meal-planner-v13`입니다.
- 배포용 압축 파일 `cook-mobile-app.zip`을 다시 생성했습니다.

## 2026-06-21

### 작업 내역 문서화

- 지금까지 진행한 작업을 날짜별로 정리했습니다.
- 현재 문서 `WORKLOG.md`를 추가했습니다.
- 이후 작업부터는 작업 완료 시 `WORKLOG.md`도 함께 업데이트하기로 정했습니다.

## 2026-06-23

### 탭 기반 화면 구조 개선

- 긴 단일 스크롤 화면을 탭 기반 구조로 변경했습니다.
- 탭 순서를 다음과 같이 구성했습니다.
  - 식단
  - 식단짜기
  - 재료관리
  - 레시피관리
  - 사진
- 식단이 아직 생성되지 않은 상태에서는 `식단짜기` 탭이 먼저 열리도록 했습니다.
- 식단 생성이 완료되면 자동으로 `식단` 탭으로 이동하도록 했습니다.
- 재료 없이 식단을 만들려고 하면 `재료관리` 탭으로 이동하고 냉장 재료 입력칸에 포커스가 가도록 수정했습니다.
- 기존 폼과 입력 로직은 유지하고, 앱 시작 시 각 섹션을 탭 패널로 재배치하는 방식으로 구현했습니다.
- 탭 모드에서는 바깥 패널 장식을 제거해 중첩 카드처럼 보이지 않게 조정했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v14`로 갱신했습니다.

### 모바일 탭 바 사용성 개선

- 모바일에서 탭 바의 가로 스크롤을 제거했습니다.
- 탭 버튼을 3열 그리드로 배치해 전체 탭을 한눈에 볼 수 있도록 수정했습니다.
- 모바일 탭 바가 상단 앱바 아래에 고정되도록 위치를 조정했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v15`로 갱신했습니다.

### 레시피 상세 보기와 MD 파일 보관

- 전체 레시피 관리와 내 레시피 목록에서 레시피 항목을 클릭하면 상세 정보를 볼 수 있도록 수정했습니다.
- 상세 보기에는 상세 재료, 예상 재료가격, 만드는 시간, 난이도, 음식 카테고리, 만드는 방법, 참조 레시피 링크를 표시하도록 했습니다.
- 기본 레시피 1,420개를 각각 별도 MD 파일로 생성했습니다.
- MD 파일 저장 위치는 `recipe-md/`입니다.
- 각 기본 레시피 데이터에 `detailFile` 경로를 연결했습니다.
- 새 레시피 파일의 표준 형식을 `RECIPE_TEMPLATE.md`로 추가했습니다.
- 상세 보기에서 기본 레시피는 저장된 MD 파일을 열 수 있고, 내 레시피는 같은 템플릿의 MD 파일을 다운로드할 수 있게 했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v16`으로 갱신했습니다.

### 네이버 블로그/유튜브 중심 참조 레시피 보강

- 만개의레시피를 대표 참조로 쓰지 않도록 방향을 변경했습니다.
- 레시피별 대표 참조를 네이버 블로그 검색 상위 결과와 유튜브 조회수 기준 인기 영상 중심으로 보강했습니다.
- 네이버 블로그 대표 링크가 확인된 레시피는 1,059개입니다.
- 유튜브 대표 영상 링크가 확인된 레시피는 964개입니다.
- 대표 링크를 확정하지 못한 레시피는 네이버 블로그 검색 링크와 유튜브 검색 링크를 유지하도록 했습니다.
- 만개의레시피와 10000recipe 링크는 대표 참조에서 제외했습니다.
- `recipe-md/`의 레시피별 MD 파일을 새 참조 링크 기준으로 다시 생성했습니다.
- 앱 상세 보기에도 네이버 블로그 링크와 유튜브 인기 영상 링크를 표시하도록 수정했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v17`로 갱신했습니다.

### 내 레시피 조리 내용 입력과 식단 상세보기 연결

- 내 레시피 추가/수정 폼에 만드는 방법 입력칸을 추가했습니다.
- 직접 입력한 만드는 방법은 레시피 상세보기와 MD 다운로드 내용에 함께 반영되도록 했습니다.
- 기본 레시피 수정 시 기존 조리 단계가 입력칸에 불러와지도록 했습니다.
- 식단 카드에 `레시피 상세보기` 버튼을 추가해 상세 재료, 예산, 난이도, 조리시간, 만드는 방법을 바로 확인할 수 있게 했습니다.
- 식단 카드의 네이버/유튜브 링크 문구를 출처가 아닌 `추가 참조`로 정리했습니다.
- 기본 레시피에 MD 파일 경로가 있으면 식단 카드와 상세보기에서 `MD 원본 파일`을 열 수 있게 했습니다.
- GitHub Pages 업로드 시 최소 업로드 파일과 `recipe-md/` 전체 업로드 기준을 `DEPLOY.md`에 정리했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v18`로 갱신했습니다.

### 재료 삭제 저장 문제와 샘플 버튼 정리

- 재료 칩 삭제를 재료명 문자열 기준이 아니라 보관칸별 순번 기준으로 처리하도록 수정했습니다.
- 재료명에 공백, 특수문자, 따옴표 등이 있어도 삭제 후 저장이 안정적으로 유지되도록 했습니다.
- 실사용 화면에서 혼동을 줄이기 위해 `샘플 채우기` 버튼과 예시 재료 자동 주입 로직을 제거했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v19`로 갱신했습니다.

### 재료관리 반찬 칸 추가

- 재료관리의 보관 방식 입력에 `반찬` 칸을 추가했습니다.
- 장조림, 멸치볶음처럼 이미 만들어진 반찬을 재료와 분리해서 기록할 수 있도록 했습니다.
- 반찬 칸에 입력한 항목도 식단 생성 시 보유 재료 후보에 포함되도록 연결했습니다.
- 기존 저장 데이터와 충돌하지 않도록 `side` 저장칸을 새로 추가했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v20`으로 갱신했습니다.

### 기기 간 데이터 백업/불러오기 추가

- 핸드폰 홈화면 앱과 PC 브라우저가 자동 동기화되지 않는 localStorage 구조임을 확인했습니다.
- 재료관리 탭에 `데이터 백업 / 불러오기` 영역을 추가했습니다.
- 백업 파일에는 보유 재료, 반찬, 밀키트/완제품, 내 레시피, 기본 레시피 수정/삭제 내역, 마지막 식단, 식단 설정값이 포함됩니다.
- 식단 생성 시 마지막 식단을 브라우저 저장소에 자동 저장하도록 수정했습니다.
- 앱을 다시 열어도 마지막 식단이 남아 있으면 식단 탭에서 바로 볼 수 있게 했습니다.
- 다른 기기에서 백업 JSON 파일을 불러오면 해당 기기의 앱 데이터가 백업 내용으로 갱신되도록 했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v21`로 갱신했습니다.

### 간편 동기화 링크 추가

- 파일을 직접 옮기기 어려운 상황을 위해 `동기화 링크 만들기` 버튼을 추가했습니다.
- 핸드폰에서 동기화 링크를 만들면 공유창 또는 클립보드 복사로 카톡/메모/메일 등에 보낼 수 있게 했습니다.
- 다른 기기에서 `#sync=`가 포함된 동기화 링크를 열면 백업 데이터가 자동으로 적용되도록 했습니다.
- 링크 적용 후 주소의 동기화 데이터를 자동으로 제거해 긴 주소가 계속 남지 않도록 했습니다.
- 동기화 링크가 너무 길어질 경우 백업 파일 방식을 안내하도록 했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v22`로 갱신했습니다.

### Firebase Google 자동 동기화 추가

- Firebase 설정값을 앱에 연결하고 Google 로그인 기반 자동 동기화 기능을 추가했습니다.
- 재료, 반찬, 밀키트/완제품, 내 레시피, 기본 레시피 수정/삭제 내역, 마지막 식단, 식단 설정값을 Firestore에 저장하도록 했습니다.
- 같은 Google 계정으로 핸드폰과 PC에서 로그인하면 Firestore 데이터를 자동으로 불러오고 변경사항을 자동 저장하도록 했습니다.
- 동기화 상태 표시와 로그아웃 버튼을 추가했습니다.
- Firestore 보안 규칙 안내 파일 `FIREBASE-RULES.md`를 추가했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v23`으로 갱신했습니다.

### 동기화 화면 단순화

- Firebase 자동 동기화를 사용하므로 재료관리 탭의 링크/파일 동기화 버튼을 화면에서 제거했습니다.
- 동기화 영역 문구를 Google 계정 자동 동기화 중심으로 정리했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v24`로 갱신했습니다.

### 자동 동기화 범위 문구 명확화

- 자동 동기화가 재료만이 아니라 앱 데이터 전체에 적용된다는 점을 화면에 명확히 표시했습니다.
- 동기화 항목을 재료, 반찬, 밀키트, 내 레시피, 기본 레시피 수정/삭제, 마지막 식단, 식단 설정으로 안내했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v25`로 갱신했습니다.

### Firebase 로그인 오류 표시 개선

- Google 자동 동기화 로그인 실패 시 Firebase 오류 코드를 함께 표시하도록 수정했습니다.
- 허용 도메인, Google 로그인 제공자 설정 등 Firebase 콘솔 설정 문제를 더 쉽게 구분할 수 있게 했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v26`으로 갱신했습니다.

## 주요 산출물

| 파일 | 설명 |
|---|---|
| `index.html` | 앱 화면 구조 |
| `styles.css` | 모바일 UI와 파스텔 디자인 |
| `app.js` | 식단 생성, 레시피 관리, 예산 계산, 캘린더 연동 로직 |
| `recipes-data.json` | 대량 레시피 원본 데이터 |
| `recipes-data.js` | JSON 로딩 실패 시 사용하는 대량 레시피 스크립트 |
| `recipe-md/` | 레시피별 상세 MD 파일 |
| `RECIPE_TEMPLATE.md` | 새 레시피 MD 파일 표준 템플릿 |
| `recipes-data-summary.json` | 레시피 데이터 요약 |
| `manifest.json` | 홈화면 앱 설정 |
| `sw.js` | 서비스워커 캐시 |
| `app-icon.svg` | 홈화면 앱 아이콘 |
| `PHONE-SETUP.md` | 휴대폰 사용 안내 |
| `DEPLOY.md` | 배포 안내 |
| `cook-mobile-app.zip` | 업로드/백업용 압축 파일 |

## 현재 앱 상태 요약

- 휴대폰 홈화면 앱처럼 사용할 수 있습니다.
- 냉장/냉동/실온/소스(양념)/반찬/라면/과일 및 디저트별로 재료를 칩 형태로 관리할 수 있습니다.
- 밀키트/완제품도 별도 칩으로 입력할 수 있습니다.
- 점심과 저녁 기준으로 일주일 식단을 생성합니다.
- 난이도와 카테고리 비중을 조절할 수 있습니다.
- 직접 만든 레시피를 추가할 수 있습니다.
- 기본 레시피를 검색, 수정, 삭제, 복구할 수 있습니다.
- 추가 구매 재료의 대략 예산과 가격 확인 링크를 제공합니다.
- 일주일 식단을 기본 캘린더에 넣을 수 있습니다.

## 2026-06-29 꼬붕이 냉장고 앱 규칙 연동 보강

- 꼬붕이가 냉장고 앱에 레시피를 추가할 때 Firebase 데이터만 저장하지 않고 `recipe-md/` 아래에 레시피별 MD 파일을 생성하도록 보강했습니다.
- 새 레시피 데이터에 `detailFile`을 함께 저장하여 앱의 레시피 상세보기/MD 원본 링크 규칙을 유지하도록 했습니다.
- MD 파일은 기존 레시피 템플릿 구조에 맞춰 기본 정보, 상세 재료, 만드는 시간, 난이도, 예상 재료비, 만드는 방법, 참조 링크, 관리 메모를 포함합니다.
- 꼬붕이의 사진 재료 입력용 일괄 추가 도구도 냉장/냉동/실온보관/양념류/반찬/밀키트 카테고리를 명확히 요구하도록 유지했습니다.

## 2026-07-17 재료 관리 입력 UX 보강

- 재료 관리에 `라면`, `과일 및 디저트` 입력칸을 추가했습니다.
- 라면은 식단 생성 시 밀키트처럼 메인 후보로 들어가고, 과일 및 디저트는 각 끼니의 후식으로 표시되도록 연결했습니다.
- 재료/밀키트 칩은 클릭 즉시 삭제되지 않고 선택 상태만 바뀌며, `선택 삭제` 버튼을 눌렀을 때만 삭제되도록 변경했습니다.
- `양념류` 명칭을 `소스(양념)`으로 변경했습니다.
- 입력한 재료를 찾을 수 있는 검색창과 검색 결과 요약을 추가했습니다.
- 재료 칩 색상을 초록 계열에서 파스텔 옐로우/핑크 계열로 조정했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v27`로 갱신했습니다.

## 2026-07-18 전체 색상 팔레트 정리

- 앱 전반에 남아 있던 민트/초록 계열 강조색을 제거하고 파스텔 핑크, 코랄, 라벤더, 크림 계열로 통일했습니다.
- 기본 버튼, 활성 탭, 입력 포커스, 모바일 상단바, OCR 카드와 요약 카드의 강조색을 새 팔레트에 맞게 조정했습니다.
- 서비스워커 캐시 버전을 `fridge-meal-planner-v28`로 갱신했습니다.
